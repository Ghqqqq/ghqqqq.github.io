# Homepage Integration

Local Astro integration used by this site to expose shared metadata to pages.

The integration validates configuration in `astro.config.ts` and provides the
values through a virtual module consumed by the application.
